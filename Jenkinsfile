node {
    stage 'Empty'
    
    sh "ls | echo 'There is no content, the folder is empty.'"
    
    stage 'Create build output'
    
    sh "mkdir -p output"
    
    stage "Archive build output"
    
    sh "ls "
}
